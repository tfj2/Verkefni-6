{
  "name": "verkefni-6",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "browser-sync": "browser-sync start --server",
    "sass": "node-sass --source-map-embed styles.scss styles.css",
    "sass-watch": "node-sass --source-map-embed --watch *.scss --recursive styles.css",
    "dev": "concurrently npm:sass-watch npm:browser-sync --files index.html",
    "lint-css": "stylelint styles.scss ./**/*.scss"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.26.12",
    "concurrently": "^5.3.0",
    "stylelint": "^13.7.2",
    "stylelint-config-standard": "^20.0.0"
  },
  "dependencies": {
    "node-sass": "^4.14.1"
  }
}
